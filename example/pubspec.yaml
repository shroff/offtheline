name: example
description: Example app for offtheline package
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.5 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  offtheline:
    path: ../
  flutter_state_notifier:
  hive:
  hive_flutter:
  isar: 3.0.0-dev.8
  isar_flutter_libs: 3.0.0-dev.8
  http:
  package_info:
  provider:
  state_notifier:
  uri:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner:
  isar_generator: 3.0.0-dev.8

flutter:
  uses-material-design: true